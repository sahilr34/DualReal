public static class GameSessionState
{
    // True after the player has successfully used a revive. 
    public static bool reviveUsed = false;

    // Stores the gameplay scene currently being played. 
    public static string currentGameScene = "";
}
